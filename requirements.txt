# Streamlit Community Cloud lee este fichero en la raíz del repo.
# Dependencias del dashboard (app/dashboard.py) — misma lista que app/requirements.txt.
streamlit>=1.36
pandas>=2.0
numpy>=1.26
plotly>=5.20
